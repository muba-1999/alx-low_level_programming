#include <stdio.h>

/**
 * main - Entry point
 * @argc: argument count sotres number of commands passed by the user
 * @argv: argument vector 1 dimension array
 *
 * Return: Always 0 (Success)
 */
int main(int argc, char *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);
	return (0);
}
