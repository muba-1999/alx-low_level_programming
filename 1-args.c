#include <stdio.h>

/**
 * main - Entry point
 * @argc: argument counter
 * @argv: argument vector array
 *
 * Return: Always 0 (Success)
 */
int main(int argc, char *argv)
{
	printf("%d\n", argc);

	return (0);
}
