#include "holberton"

/**
 * _isalpha - checks id an argument is a letter
 * @c - writes the value to the stdout
 *
 * Return: on True 1
 * Return: on False 0
 */
int _isalpha(int c)
{
	if ((c == 'A' && c == 'Z') || (c == 'a' && c == 'z'))
	{
		return (0);
	}
	else
	{
		return (0);
	}
}
