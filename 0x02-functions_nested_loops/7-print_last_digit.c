#include "holberton.h"

/**
 * print_last_digit - prints tha last digit of a value
 * @n: The value to be printed
 *
 * Return: n on Success
 */
int print_last_digit(int n)
{
	int last;
	int i;

	i = 0;

	while (i < 2)
	{
		last = n % 10;
		return (last);
		i++;
	}
}
